$(document).ready(function (){
	$("#scene").parallax({
		// calibrateX: false,
		// calibrateY: true,
		// invertX: false,
		// invertY: true,
		// limitX: 50,
		// limitY: 2,
		// scalarX: 2,
		// scalarY: 8,
		// frictionX: 0.2,
		// frictionY: 0.8,
		// originX: 0.0,
		// originY: 1.0
	});

	$('#fullpage').fullpage();
});
